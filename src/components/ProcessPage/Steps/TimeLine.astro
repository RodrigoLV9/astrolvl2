<div class="timeline">
    <div class="timeline-circle">1</div>
    <div class="timeline-circle">1</div>
    <div class="timeline-circle">1</div>
    <div class="timeline-circle">1</div>
    <span class="timeline-bar"></span>
</div>
<style>
    .timeline{
        position:relative;
        display: flex;
        flex-direction: column;
        align-items:center;
        justify-content: space-between;
        gap:50px;
        width: 80vw;
        height: 1200px;
        margin:auto;
    }
    .timeline-circle{
        position:relative;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 40px;
        height: 40px;
        background-color: var(--clr-primary-a55);
        border-radius: var(--rad-full);
        font-size: var(--text-lg);
        z-index: 3;
    }
    .timeline-circle::before{
        content: "";
        display: block;
        width:70px;
        height:70px;
        background-color: var(--clr-primary-a20);
        position: absolute;
        z-index:2;
        margin: auto;
        border-radius: var(--rad-full);
    }
    .timeline-bar{
        height: 1200px;
        width: 3px;
        background-color: var(--clr-primary);
        position:absolute;
        left:50%;
    }
</style>